server:
  port: 8081
  servlet:
    context-path: /api

spring:
  application:
    name: CaffeineDemo

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/caffeine_demo?useSSL=false&serverTimezone=UTC
    username: root
    password: root

  redis:
    host: liaocyu.cloud
    port: 6379
    database: 0
    timeout: 10000ms
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0
    password: 779500
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  type-aliases-package: com.cyu.domain
#logging:
#  level:
#    com.hmdp: debug
